<div id="post-<%= post.id %>" class="card">
  <div class="card-title">
    <%= post.user.display_name %>
  </div>
  <div class="card-body">
    <span id="post-thumbsup-<%= post.id %>" class="label label-success"><%= post.likes.count %> 👍</span>
    <%= post.content %>

    <p class="text-right">
      <span id="post-like-<%= post.id %>">
        <%= render :partial => "like", :locals => { :post => post } %>
      </span>
    </p>

    <% if current_user && post.user == current_user %>
      <p class="text-right">
        <%= link_to "Delete", post_path(post), :class => "btn btn-danger delete-post" %>
      </p>
    <% end %>
  </div>
</div>